import InstallationPermitReport from './installation_permit_report.mdx'

<>
    <p>**Installer:**</p>

    <p className="margin-left">
        <ShowOrHide visible={props.project}>
            <LabelValue
                path="installer.name"
                parent={props.project}
                suffix={<br />}
            />
            <LabelValue
                path="installer.company_name"
                parent={props.project}
                suffix={<br />}
            />
            <LabelValue
                path="installer.mailing_address"
                parent={props.project}
                suffix={<br />}
            />
            <LabelValue
                path="installer.phone"
                parent={props.project}
                suffix={<br />}
            />
            <LabelValue
                path="installer.email"
                parent={props.project}
                suffix={<br />}
            />
        </ShowOrHide>
        <ShowOrHide visible={props.project == null}>
            <LabelValue path="installer.name" suffix={<br />} />
            <LabelValue path="installer.company_name" suffix={<br />} />
            <LabelValue path="installer.mailing_address" suffix={<br />} />
            <LabelValue path="installer.phone" suffix={<br />} />
            <LabelValue path="installer.email" suffix={<br />} />
        </ShowOrHide>
    </p>
    <ShowOrHide visible={props.data.install_date}>
            <LabelValue value={props.data.install_date} label="Installation Date" type="date" />
    </ShowOrHide>

    <ShowOrHide visible={props.project?.data_.location}>
        **Installation Address:**

        <LocationStr
            location={props.project.data_.location}
            separators={[', ', ', ', ' ']}
        />
    </ShowOrHide>

    <Photo
        id="building_number_photo"
        label="Building Number"
        parent={props.project}
        required
    >
        A photo of the building showing the building number.
    </Photo>

    <Repeatable
        path="project_documentation"
        label="Other Project Documentation"
        parent={props.project}
    >
        <Photo
            id="photo_input"
            label=""
            parent={props.project}
        />
        <PDFRenderer id="file_input" label="" parent={props.project} />
    </Repeatable>


    <InstallationPermitReport {...props} />

</>
